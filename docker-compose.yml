services:
  redis:
    image: "redis:7.0.2"
    restart: always
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - redis_data:/data
    command: "redis-server --save 60 1 --loglevel warning"

  bot:
    build: .
    restart: always
    ports:
      - "127.0.0.1:8443:8443"
    environment:
      - POKERBOT_TOKEN=${POKERBOT_TOKEN}
      - POKERBOT_REDIS_HOST=redis
      - POKERBOT_PREFERRED_MODE=webhook
      - POKERBOT_WEBHOOK_LISTEN=0.0.0.0
      - POKERBOT_WEBHOOK_PORT=8443
      - POKERBOT_WEBHOOK_PATH=/telegram/webhook
      - POKERBOT_WEBHOOK_PUBLIC_URL=https://poker.shahin8n.sbs/telegram/webhook
      - POKERBOT_WEBHOOK_SECRET=${POKERBOT_WEBHOOK_SECRET:-}
    depends_on:
      - redis

  webapp-api:
    build:
      context: ./webapp-backend
      dockerfile: Dockerfile
    restart: always
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      - REDIS_HOST=${REDIS_HOST}
      - REDIS_PORT=${REDIS_PORT}
      - WEBAPP_SECRET=${WEBAPP_SECRET}
      - TELEGRAM_BOT_TOKEN=${POKERBOT_TOKEN}
      - CORS_ORIGINS=${CORS_ORIGINS}
      - PYTHONPATH=/app
    volumes:
      # 🔥 CRITICAL FIX: Share game core with webapp-api
      - ./pokerapp:/app/pokerapp:ro
      - ./translations:/app/translations:ro
    depends_on:
      - redis
      - bot

  webapp-frontend:
    build:
      context: ./webapp-frontend
      dockerfile: Dockerfile
    restart: always
    ports:
      - "127.0.0.1:3000:80"
    depends_on:
      - webapp-api

volumes:
  redis_data:
